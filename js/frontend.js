//currently hardcoded
var userType = true; //true means Verified, false means unverified
var userNames = ["user1", "user2"]; //array of selected user names
var dates = ["date1", "date2", "date3"]; //array of all selected dates (not just *to* and *from* but all of the dates)
var colorPallete = "pallete1"; //name of the collor pallete 
var maxFontSize = 20; //integer 
var maxTags = 50; //int; limiting the amount of tags

function generate(){
    //ToDo: this should refresh the:  my_dataviz div
} 